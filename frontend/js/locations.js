const LocationsAPI = { locations: { getAll: () => fetch(`${API_BASE_URL}/locations/rumah`, { headers: getAuthHeaders() }).then(r => r.json()), create: (data) => fetch(`${API_BASE_URL}/locations/rumah`, { method: 'POST', headers: { 'Content-Type': 'application/json', 'Authorization': `Bearer ${getAuthToken()}` }, body: JSON.stringify(data) }).then(r => r.json()) } };
document.addEventListener('DOMContentLoaded', () => { loadRumah(); });
function loadRumah() { LocationsAPI.locations.getAll().then(response => { if (response.success) { renderRumah(response.data); } }); }
function renderRumah(rumahList) { const container = document.getElementById('rumahList'); if (rumahList.length === 0) { container.innerHTML = '<p class="text-gray-500 col-span-full text-center py-8">Tidak ada rumah keluarga</p>'; return; } container.innerHTML = rumahList.map(r => `<div class="bg-white rounded-lg shadow p-4"><h3 class="font-bold text-lg">${r.nama}</h3><p class="text-gray-600 text-sm">${r.alamat}</p><p class="text-gray-500 text-sm">${r.kota}, ${r.provinsi}</p><span class="px-2 py-1 text-xs rounded-full bg-blue-100 text-blue-800">${r.tipe}</span></div>`).join(); }
