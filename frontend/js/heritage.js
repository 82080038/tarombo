const HeritageAPI = { heritage: { getTraditions: () => fetch(`${API_BASE}/api/v1/heritage/traditions`).then(r => r.json()), createTradition: (data) => fetch(`${API_BASE}/api/v1/heritage/traditions`, { method: 'POST', headers: { 'Content-Type': 'application/json', 'Authorization': `Bearer ${getToken()}` }, body: JSON.stringify(data) }).then(r => r.json()), getStories: () => fetch(`${API_BASE}/api/v1/heritage/stories`).then(r => r.json()), createStory: (data) => fetch(`${API_BASE}/api/v1/heritage/stories`, { method: 'POST', headers: { 'Content-Type': 'application/json', 'Authorization': `Bearer ${getToken()}` }, body: JSON.stringify(data) }).then(r => r.json()) } };
document.addEventListener('DOMContentLoaded', () => { loadTraditions(); loadStories(); });
function loadTraditions() { HeritageAPI.heritage.getTraditions().then(response => { if (response.success) { renderTraditions(response.data); } }); }
function renderTraditions(traditions) { const container = document.getElementById('traditionsList'); if (traditions.length === 0) { container.innerHTML = '<p class="text-gray-500 text-sm">Tidak ada tradisi</p>'; return; } container.innerHTML = traditions.map(t => `<div class="border-l-4 border-purple-500 pl-3"><h3 class="font-medium">${t.nama}</h3><p class="text-gray-600 text-sm">${t.deskripsi || '-'}</p><span class="text-xs text-gray-500">${t.kategori}</span></div>`).join(); }
function loadStories() { HeritageAPI.heritage.getStories().then(response => { if (response.success) { renderStories(response.data); } }); }
function renderStories(stories) { const container = document.getElementById('storiesList'); if (stories.length === 0) { container.innerHTML = '<p class="text-gray-500 text-sm">Tidak ada cerita</p>'; return; } container.innerHTML = stories.map(s => `<div class="border-l-4 border-blue-500 pl-3"><h3 class="font-medium">${s.judul}</h3><p class="text-gray-600 text-sm line-clamp-2">${s.konten}</p><span class="text-xs text-gray-500">${s.tipe}</span></div>`).join(); }
